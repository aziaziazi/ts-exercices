# Exercices around Array and Object in TS

## Install

`pnpm install`

## Instructions

Read the doc for each function in `src/arrays/array.ts`.  
Implements the function.  
The corresponding test (in `arrays.spec.ts`) should pass.

Do not modify the test !
The implementation should be immutable !
You can use the debugger when you run a test.
